import React from "react"; // É uma biblioteca global
import ReactDOM from "react-dom";
import App from "./App";

// Pega o componente e manda ele para o div com id root
ReactDOM.render(<App />, document.getElementById("root"));
